# PLAN — спринт ~30 часов до дедлайна 13.07 включительно. Сверху вниз, `[x]` только после «Проверка:».

Правило фазы: все чекбоксы + build/test зелёные + критик (≤3 циклов) + деплой + prod-verify
зелёный (включая СТАРЫЕ проверки) → фаза закрыта → commit. Раскладка: сегодня вечер — фазы 0–3;
завтра утро — 4–6; завтра день — 7–8 и ПОДАЧА С ЗАПАСОМ ≥3 часов.

## Фаза 0 — Фундамент и анти-грабли (≈2 ч) — самая важная
- [x] 0.1 `npx create-next-app@latest sana --typescript --tailwind --eslint --app`; deps:
      `@libsql/client zod recharts @anthropic-ai/sdk bcryptjs date-fns` + dev `vitest tsx`.
      shadcn init + button card input dialog badge tabs accordion table tooltip skeleton sheet.
      Проверка: build зелёный.
- [x] 0.2 **Turso с первого коммита**: lib/data/db.ts на @libsql/client (web-клиент для
      libsql:// — win32-arm64 не тянет нативный file:, см. TODO.md). .env.example. НИКАКОГО
      better-sqlite3. Проверка select 1 — отложена до кредов Turso (0.7).
- [x] 0.3 scripts/db-deploy.mjs (идемпотентные CREATE TABLE IF NOT EXISTS по SPEC §3) +
      scripts/seed.mjs (справочники+пользователи, факты — в фазе 1). package.json: seed, test,
      prod:verify. vercel.json buildCommand: `node scripts/db-deploy.mjs && next build`.
- [x] 0.4 /api/health/db (adapter, счётчики, commit SHA) — диагностика с первого дня.
- [x] 0.5 .claude/agents/critic.md уже в репо (из кита) — проверь, что виден как субагент.
- [x] 0.6 scripts/prod-verify.mjs по SPEC §11 (проверки, которых ещё нет, пока skip-помечены).
- [ ] 0.7 **Деплой №1 и фиксация проекта**: создай ОДИН проект Vercel, имя `sana`. Пользователь
      добавит TURSO_* и ANTHROPIC_API_KEY (скоуп Production!) — выведи ему точный список имён
      переменных. После его «готово»: пуш → выведи ФАКТИЧЕСКИЙ прод-домен из деплоя → захардкодь
      его как BASE в prod-verify. Проверка: GET {BASE}/api/health/db с прода показывает Turso и
      users>0. Несовпадение домена ↔ проекта = стоп и разберись (урок «двух проектов»!).
Выходной критерий: health на проде зелёный, запись в Turso с прода работает (тест-инсерт в
audit_log через временный вызов).

## Фаза 1 — Semantic Layer + данные + компилятор (≈4 ч)
- [x] 1.1 lib/semantic/types.ts + packs/auto.ts (метрики/измерения/синонимы/12+ RU и 6+ KK
      sampleQuestions по SPEC §4) + retail/bank + registry. Проверка: тесты registry зелёные.
- [~] 1.2 seed фактов AUTO (~45k строк, PRNG seed=42, сезонность, 3 аномалии из BRIEF §4) +
      retail (~20k) + bank (~10k) + 40 строк ask_history-сида. КОД готов (seed-facts.mjs);
      запуск/проверка counts — как только придут креды Turso.
- [x] 1.3 engine/planSchema.ts (zod по SPEC §5) + валидация ссылок на пак. Тесты.
- [x] 1.4 engine/compiler.ts + runner.ts: whitelist, параметризация, compare-период, LIMIT.
      Тесты: инъекция в filter.value не попадает в SQL; метрика вне пака → ошибка 422.
- [x] 1.5 engine/deltas.ts + chartSelector.ts по SPEC §6. Тесты эталонных чисел и 6 форм графиков.
Выходной критерий: `npm run test` ≥20 зелёных; руками: compile(план «выручка по месяцам») → SQL
→ runner → 24 строки.

## Фаза 2 — AI-пайплайн /api/ask (≈4 ч)
- [x] 2.1 lib/ai/provider.ts (anthropic|mock по env, без ключа НЕ кидает — mock), prompts.ts
      дословно из SPEC §7, anthropic.ts (model из env, max_tokens 2000, JSON-repair 1 повтор).
- [x] 2.2 mock.ts: роутер синонимов → планы для ВСЕХ sampleQuestions (RU+KK) всех паков +
      шаблонные insight из deltas. Тест: каждый sampleQuestion → валидный план (57 кейсов зелёные).
- [x] 2.3 /api/ask полный пайплайн SPEC §6 (вкл. RBAC-инъекцию фильтра, историю, audit, clarify
      вместо 500 на чепухе). /api/history, /api/insights (из anomalies.ts на лету).
      Проверка «вживую» (6 вопросов + плов) — при кредах Turso; build+типы зелёные.
- [x] 2.4 Auth: cookie-сессия HMAC, /api/auth/*, bcrypt, 3 демо-пользователя.
Выходной критерий: curl /api/ask локально на 3 паках зелёный; критик фазы пройден.

## Фаза 3 — Воркспейс и дизайн (≈4 ч)
- [x] 3.1 Токены SPEC §2 в globals (тёмная тема, tnum-числа, z-index диалогов ≥1000). DESIGN.md.
- [x] 3.2 /login: три большие кнопки-роли (описание) → один клик = сессия + редирект /app.
      Проверка «вживую» — при кредах Turso (login бьёт в БД).
- [x] 3.3 /app: AskBar (Enter/кнопка, чипы вкл. KK), лента AnswerCard c этап-скелетоном,
      KpiRow/ChartAuto/InsightBlock, TrustPanel-аккордеон (план→SQL→N строк→метрики→«LLM не
      получал строки»), FollowUps-чипы, История. Экспорт PNG. Проверка «вживую» — при кредах.
- [x] 3.4 PackSwitcher + RoleBadge + плашка scope. Build+типы зелёные.
Выходной критерий: сценарий п.2–5 (вкл. казахский вопрос) проходит локально красиво.

## Фаза 4 — Embed поверх «чужой» BI (≈2.5 ч) — требование №6, наш WOW
- [x] 4.1 /embed (светлый компакт-режим) + components/widget/SanaWidget.
- [x] 4.2 public/widget.js: монтирует плавающую кнопку и iframe /embed?pack=… (честная 1 строка).
- [x] 4.3 /legacy-bi: типовой «чужой» светлый дашборд (статичные графики, шапка «Kaz-Analytics BI»)
      + подключённый widget.js + блок «Подключение — одна строка» со сниппетом и копированием.
      Проверка: вопрос из виджета на /legacy-bi возвращает ответ; выглядит как чужой продукт с
      нашей кнопкой.
Выходной критерий: п.8 демо-сценария живой.

## Фаза 5 — RBAC-демо + мультиотраслевость (≈2 ч)
- [x] 5.1 REGION-поток: тот же вопрос → данные только Алматы + плашка. Тест rbac зелёный.
      Проверка руками: CEO vs REGION дают разные топы.
- [x] 5.2 retail/bank паки полноценно в UI (подсказки, метрики). Проверка: по 3 вопроса на пак.
- [x] 5.3 ANALYST: SQL раскрыт по умолчанию + вкладка «Все запросы».
Выходной критерий: п.6–7 демо-сценария живые.

## Фаза 6 — WOW-слой и витрина (≈3 ч)
- [ ] 6.1 Лендинг / по SPEC §8: живой пример ответа, счётчики из БД (НЕ нули), карточка
      «SANA заметила» (3 авто-инсайта с мини-графиками), блок безопасности, блок «как работает».
- [ ] 6.2 Демо-тур (4 шага по воркспейсу; лёгкий, паттерн прошлого проекта: 4 dim-панели, без
      box-shadow 9999px!).
- [ ] 6.3 Экспорт AnswerCard в PNG (html-to-image), футер карточки «посчитано SANA · N строк».
- [ ] 6.4 Мобильный прогон / и /app (0px горизонтального скролла на 375px).
Выходной критерий: витрина продаёт за 5 секунд; критик не находит «дефолтного ИИ-вида».

## Фаза 7 — Репозиторий и приёмка (≈3 ч)
- [ ] 7.1 README-продающий: что это + живой стенд; запуск в 3 команды; таблица демо-доступов;
      **таблица «7 требований ТЗ → где на стенде (URL) → где в коде»**; 3–4 скриншота; стек;
      тесты. ARCHITECTURE.md (mermaid по SPEC §13) + ADD_NEW_PACK.md.
- [ ] 7.2 Чистка: console.log, мёртвые файлы, dev-заглушки; .env не в гите; поиск секретов по
      истории (`git log -p --all | grep -iE "sk-ant-|TURSO_AUTH"`) — найдёшь → доложи, не ротируй.
- [ ] 7.3 Полный prod-verify по SPEC §11 (все skip сняты) на проде — зелёный, вывод в отчёт.
      Прогрев: GitHub Actions cron-пинг главной раз в 10 минут.
- [ ] 7.4 Финальный ручной прогон демо-сценария BRIEF §7 п.1–10 на проде + скриншоты в docs/screens/.
Выходной критерий: DoD пункты 1–4 из CLAUDE.md закрыты.

## Фаза 8 — Питч и подача (≈3 ч)
- [ ] 8.1 docs/PITCH.md → PPTX+PDF (скилл pptx, прочитать SKILL.md перед сборкой). 10 слайдов:
      1 Титул (SANA · «Спросите свои данные» · «Ответ по данным за секунды — черновик решения,
      а не угадывание» · команда · QR на стенд и репо) · 2 Проблема (BI есть — инсайты у 5%
      сотрудников; путь «вопрос→аналитик→выгрузка→дни») · 3 Решение: 4 шага «вопрос → план →
      SQL → ответ+вывод» · 4 📸 воркспейс с ответом и графиком · 5 Ядро доверия: Semantic Layer,
      «LLM не пишет SQL и не видит строк», 📸 TrustPanel · 6 📸 Embed поверх существующей BI +
      сниппет 1 строка · 7 Безопасность: RBAC (📸 CEO vs регион), AuditLog, контур, on-prem-ready ·
      8 Мультиотраслевость: 3 пака на одном ядре, «новый пак за 30 минут» · 9 Это работает:
      метрики стенда (строк данных, медиана ответа мс, N вопросов, тесты) — ЦИФРЫ СВЕРИТЬ СО
      СТЕНДОМ в момент сборки · 10 Пилот: «2 недели на ваших данных: доступ к витрине + 1 встреча
      по метрикам» + контакты. Правила прошлого раза: ≤5 строк/слайд, палитра SANA, реальные
      скриншоты, слово «mock» отсутствует, уязвимых обещаний нет.
- [ ] 8.2 docs/DEMO_SCRIPT.md: поминутный сценарий 5 мин + план Б (запись экрана заранее).
- [ ] 8.3 Чек-лист подачи: ссылка на стенд ✓ · репозиторий доступен (подтвердить) ✓ ·
      презентация PDF/PPTX ✓ · статус в форме: MVP. Вывести пользователю финальный отчёт:
      ссылка репо, сырой prod-verify, список закрытых требований ТЗ 1–7, что осталось.
Выходной критерий: все материалы готовы ≥3 часов до конца приёма.
